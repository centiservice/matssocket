// MatsSocket-Client-JavaScript

// Nothing here - go to subprojects 'client', 'tests_esm' and 'test_umd'

// Test: Depend on both test subprojects' tests
tasks.register('test') { dependsOn 'tests_esm:test', 'tests_umd:test' }