// MatsSocket-Client-JavaScript

// Nothing here - go to subprojects 'client' and 'tests'.

// Test: Depend on both test subprojects' tests
tasks.register('test') { dependsOn 'tests:test' }
tasks.register('build') { dependsOn 'client:build', test }
